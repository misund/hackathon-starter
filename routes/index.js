const apiExamplesRouter = require('./apiExamplesRouter');
const authRouter = require('./authRouter');
const userRouter = require('./userRouter');

const routers = {
  apiExamplesRouter,
  authRouter,
  userRouter,
};

module.exports = routers;
